apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-reactive-cosmos
  labels:
    app: cosmos
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cosmos
  template:
    metadata:
      labels:
        app: cosmos
    spec:
      containers:
        - name: spring-reactive-cosmos
          image: acrintegrations01.azurecr.io/aks/spring-reactive-cosmos:latest
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: cloud
          ports:
            - containerPort: 8760